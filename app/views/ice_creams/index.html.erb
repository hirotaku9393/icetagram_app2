
<div class="pt-10 flex justify-center font-kiwi gap-10 mt-6">
    <%= link_to new_ice_cream_path, class: "bg-stone-300 hover:bg-stone-400 text-stone-800 font-bold py-2 px-4 rounded flex items-center gap-2 w-68" do %>
        <iconify-icon icon="twemoji:ice-cream" width="36" height="36"></iconify-icon>
        <span>あなたの推しあいすを追加</span>
    <% end %>
</div>

<div class="mx-auto max-w-7xl px-4">
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-2 mt-6">
        <% @ice_creams.each do |ice_cream| %>
            <div class="bg-white shadow-md rounded-lg p-4 ">
                <h2 class="font-kiwi text-xl"><%= ice_cream.name %></h2> 
                <p class="text-gray-700 font-kiwi"><%= ice_cream.comment %></p>
                <div class="mt-2 flex justify-center">
                    <% if ice_cream.image.attached? %>
                        <div class="w-40 aspect-[16/9] flex items-center justify-center">
                            <%= image_tag ice_cream.image, class: 'max-h-full max-w-full object-contain' %>
                        </div>
                    <% else %>  
                            <p class="text-gray-500">画像がまだありません</p>
                    <% end %>
                </div>
                <div class="mt-4">
                    <%= link_to 'アイスの詳細', ice_cream_path(ice_cream), class: 'bg-stone-300 hover:bg-stone-400 text-stone-800 text-lg font-kiwi py-2 px-4 rounded-lg' %>  
                </div>
            </div>
        <% end %>
    </div>
</div>

<div class="flex flex-wrap gap-2 justify-center mt-6">
    <%= paginate @ice_creams %>
</div>
