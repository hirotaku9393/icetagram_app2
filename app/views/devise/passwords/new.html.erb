<h2 class="pt-30 font-kiwi text-4xl text-black-60 text-center">パスワードをお忘れですか?<h2>

<div class="mx-auto bg-amber-50 shadow-md rounded-lg p-6 max-w-md mt-6 text-center">
<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field my-4">
    <%= f.label :email, class: "w-64 text-left font-kiwi text-gray-700" %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email",  class: "px-10 py-2  border border-gray-300 rounded-md " %>
  </div>

  <div class="actions my-4">
    <%= f.submit "パスワードリセットメールを送信", class: "w-65 bg-stone-300 hover:bg-stone-400 text-stone-800 font-kiwi py-2 px-4 rounded" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>